<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusactivity.mapper.AnnouncementMapper">
    <resultMap id="BaseResultMap" type="com.campusactivity.entity.Announcement">
        <id column="announcement_id" property="announcementId" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        announcement_id, title, content, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.campusactivity.entity.Announcement">
        INSERT INTO announcement
        (title, content, status, create_time, update_time)
        VALUES
        (#{title}, #{content}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.campusactivity.entity.Announcement">
        UPDATE announcement
        SET
        title = #{title},
        content = #{content},
        status = #{status},
        update_time = NOW()
        WHERE announcement_id = #{announcementId}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        DELETE FROM announcement WHERE announcement_id = #{announcementId}
    </delete>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM announcement
        WHERE announcement_id = #{announcementId}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM announcement
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM announcement
        WHERE status = #{status}
        ORDER BY create_time DESC
    </select>
</mapper>
