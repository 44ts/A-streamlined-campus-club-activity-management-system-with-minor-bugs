<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusactivity.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.campusactivity.entity.User">
        <id column="user_id" property="userId" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="role" property="role" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="avatar" property="avatar" />
    </resultMap>

    <sql id="Base_Column_List">
        user_id, username, password, email, role, status, create_time, update_time, avatar
    </sql>

    <insert id="insert" parameterType="com.campusactivity.entity.User">
        INSERT INTO user
        (username, password, email, role, status, avatar, create_time, update_time)
        VALUES
        (#{username}, #{password}, #{email}, #{role}, #{status}, #{avatar}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.campusactivity.entity.User">
        UPDATE user
        SET
        update_time = NOW()
        <if test="username != null">
            , username = #{username}
        </if>
        <if test="email != null">
            , email = #{email}
        </if>
        <if test="role != null">
            , role = #{role}
        </if>
        <if test="status != null">
            , status = #{status}
        </if>
        <if test="avatar != null">
            , avatar = #{avatar}
        </if>
        <if test="password != null and password != ''">
            , password = #{password}
        </if>
        WHERE user_id = #{userId}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="selectByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user
        WHERE username = #{username}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user
    </select>

    <select id="selectByRole" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user
        WHERE role = #{role}
    </select>

    <select id="selectByStatus" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user
        WHERE status = #{status}
    </select>
</mapper>
